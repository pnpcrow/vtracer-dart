name: vtracer
description: >-
  Pure Dart port of the vtracer raster-to-vector conversion framework:
  pluggable frontends, curve fitters, color fitting, and output optimization.
version: 1.0.0-alpha.4
publish_to: none

environment:
  sdk: ^3.13.0

dependencies:
  visioncortex:
    path: ../visioncortex

dev_dependencies:
  test: ^1.25.0

resolution: workspace
